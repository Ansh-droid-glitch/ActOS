[project]
name = "agentic-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ansh-droid-glitch", email = "anshsingh131014@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.2",
    "ultralytics",
]

[project.scripts]
agentic-ai = "agentic_ai:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
